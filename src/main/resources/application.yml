spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: local
  h2:
    console:
      enabled: true

---
spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_batch
    username: does
    password:
